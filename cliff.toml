[changelog]
header = """
# 更新日志

本项目的所有重要变更都记录在此文件中。

项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
"""

body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [未发布]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits 
       | filter(attribute="scope")
       | sort(attribute="scope") %}
        - **{{commit.scope}}:** {{ commit.message | upper_first }} ([{{ commit.id | truncate(length=7, end="") }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/commit/{{ commit.id }}))
    {%- endfor -%}
    {% for commit in commits %}
        {%- if not commit.scope -%}
        - {{ commit.message | upper_first }} ([{{ commit.id | truncate(length=7, end="") }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/commit/{{ commit.id }}))
        {% endif -%}
    {% endfor %}
{% endfor %}\n
"""

footer = """
---
*此更新日志由 [git-cliff](https://github.com/orhun/git-cliff) 自动生成*
"""

trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_preprocessors = [
    { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "" },
    { pattern = ".*\n\n(.|\n)*", replace = "" },
]
commit_parsers = [
    { message = "^feat", group = "🚀 新功能" },
    { message = "^fix", group = "🐛 问题修复" },
    { message = "^doc", group = "📚 文档" },
    { message = "^docs", group = "📚 文档" },
    { message = "^perf", group = "⚡ 性能优化" },
    { message = "^refactor", group = "♻️ 代码重构" },
    { message = "^style", group = "🎨 代码风格" },
    { message = "^test", group = "🧪 测试" },
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore", group = "🔧 其他变更" },
    { message = "^ci", group = "👷 CI/CD" },
    { message = "^build", group = "📦 构建系统" },
    { message = "^revert", group = "⏪ 撤销变更" },
]
protect_breaking_commits = false
filter_commits = false
tag_pattern = "v[0-9].*"
ignore_tags = ""
topo_order = false
sort_commits = "oldest"

[remote.github]
owner = "fengyec2"
repo = "custom-seewo-splash-screen"
